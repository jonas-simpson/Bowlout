using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TweenStart : TweenUI
{
    // Start is called before the first frame update
    void Start()
    {
        Animate();
    }
}
